const Cal=(props)=>{
    // let a=10;
    console.log(props);
    let c=parseInt(props.a);
    let d=parseInt(props.b);
    // let b=20;
    let {a,b}=props;
    console.log(a,b)
    return(
    <>.
    <p>This is calculator</p>
    <p>sum={c+d}</p>
    <p>sum={parseInt(props.a)+parseInt(props.b)}</p>
    </>
    ) 
  
  }
  export default Cal;